# 变更记录

## 2026-09-19 — 可关闭的 GM 见闻模式

- 新增 `--gm` / `TINY_TIDES_GM=1` 构建开关和 GM 事件选择器；可指定 10 种任意见闻或随机抽取一项，自动布置合适的时段与天气并立即演出。
- GM 强制演出会结束当前事件、绕过天气、冷却、槽位和事件链前置；预览标记阻止发现、徽章、纪念物、完成日志及导演冷却写入，不污染正式进度。
- 普通网页和桌面构建默认关闭 GM，并从生成 HTML 删除面板和触发逻辑；开启与关闭命令记录在 README。新增真实导演和环境测试，覆盖强制替换、任意条件、无进度写入、环境即时布置与世界时间不变。
- 远程目标为新建的 `sailing` 仓库；合并时保留远程原有 `LICENSE`。

## 2026-09-19 — 自动见闻第五阶段

- 新增 `encounter-catalog.mjs`、`encounter-director.mjs`、`encounter-visuals.mjs`：统一 10 种临时见闻、随机牌组/空牌、冷却与安静期、天气时段条件、一个主要加一个日常事件的上限及极光互斥。原海豚独立循环退出实际运行路径。
- 实现鱼群、伴游海豚、粉色来客、巨鲸之影及后续浮出、迁徙鸟群、荧光海、局部流星、浮冰北极熊、雾中灯塔。沿用原 Three.js 场景、海豚与水面；批量鱼鸟和固定光迹/流星/喷水缓冲复用，结束隐藏并停止更新，无新依赖。
- 存档升为版本 4，增加发现历史、完成次数/事件链、纪念物归属与导演时钟/牌组/冷却。绘制可见后记录，不要求领取；重复不重复解锁，不扣航行值。完成和中断分别发事件，包含下一阶段日志所需字段；未增加日志或纪念物装备 UI。
- 开发独立存档和 `triggerEncounter` / `DEBUG_ENCOUNTER_SPEED` 支持快速验收；加速也缩短已安排的等待。正式桌面输入移除调试入口。隐藏、暂停与休眠间隔不补算演出，重载不恢复半途特效。
- 浏览器发现并修正水面鲸影破面、未激活流星黑线/越界；细分波面投影网格、透明线段渐隐，并增加鱼群荧光接触尾迹。实际测试与限制见 `TEST_RESULTS.md`，开发说明见 README。
- 回退基点 `1d538aa`；保留升级前版本 3 存档备份，旧程序不识别版本 4。本次重建网页预览与桌面前端输入，不更新旧 EXE、安装包、线上站点或历史 HTML。

## 2026-09-19 — 环境音默认开启

- 网页预览和桌面新设置默认启用原有低音量环境音；已有桌面静音偏好继续保留。
- 浏览器限制自动播放时，首次点击/按键恢复；统一声音按钮处理，避免默认开启但尚未播放时第一次点击反而关闭设置。手动静音后不被其他交互重新开启。
- 当前开发预览已经开启播放；没有重编译现有 EXE。

## 2026-09-19 — 体验徽章第四阶段

- 建立 10 项统一徽章配置、轻量世界事件入口、去重解锁与首次日期/来源记录。海豚、极光、暴雨、黄昏接入实际绘制后的可见状态；累计航行 1/10 小时与夜间累计 10 分钟可自动解锁。
- 徽章面板显示隐藏、已知未解锁、已拥有三类；已发现详情、淡蓝 NEW、无成本佩戴/取下、双侧船身约 0.3 秒平滑切换。无额外背景提醒，不扣航行值。
- 统一存档升为版本 3，保留船名、配色、颜色归属和航行数据；仅迁移实际已佩戴旧测试徽章，不虚构历史日期。未保存成功的经历在本次运行中限频重试。
- 开发控制台解锁/事件模拟与独立测试存档短阈值；正式构建移除调试入口。鲸鱼、海玻璃和未知内容仅配置/接口占位。
- 验证记录见 TEST_RESULTS.md；本次未重编译 EXE，历史 HTML 和原交付安装包未变。

## 2026-09-19 — 颜色解锁第三阶段
- 新增统一 `color-catalog.mjs`：8 种颜色，奶油白/海军蓝/天空蓝默认拥有；薄荷绿 60、淡黄色 80、珊瑚橙 100、灰蓝 120、墨绿色 160 航行值。解锁后全船通用，归属与装备分开保存。
- 颜色页改为船体/船顶/装饰线选择与 8 张色卡，显示当前、已拥有或价格；未拥有颜色先轻量确认，取消不扣款，余额不足显示 1.8 秒说明。成功后自动装备并更新余额，无额外商城页面。
- `color-purchases.mjs` 校验颜色/部位/所有权，经统一航行值接口扣款；余额、归属与装备一次写入存档，保存失败不提交扣款或外观，重复点击不重复收费。
- 整体存档内部升级版本 2，新增 `ownedColors` 和一次性说明状态；保留键名与航行数据，自动拥有旧玩家当前配色。船体颜色用同一批材质做 0.3 秒插值，颜色变化不重绘铭牌纹理。随船预览、真实场景环绕和返回镜头保持。
- 开发独立测试存档支持 `?sailingDebug=fast&debugPoints=1000` 首次创建时增加测试点数；正式桌面构建忽略参数，普通 UI 无调试按钮。
- 验证：42 项自动检查通过；实测旧存档迁移、余额不足、1002→942 解锁、跨部位免费装备和刷新恢复；560×540 与 390×700 布局检查，纹理数量保持 2，控制台无警告/错误。开发与桌面前端构建通过，本次没有重编 EXE 或更新安装包。
- 文件：上述新模块、`colors.test.mjs`、`game-save.mjs`、`sailing.mjs`、`ship-customization.mjs`、`ship-showcase.mjs`、`main.js`、`template.html` 与开发预览。回退基点 `44334e8`；回退前备份版本 2 存档，旧代码仅支持版本 1，恢复旧数据需要升级前备份。

## 2026-09-19 — 随船预览与真实场景环绕
- 按最新交互要求，进入「我的船」后航线继续推进，尾流持续生成，近景相机跟随小船；手动暂停仍优先生效。
- 展示拖动从修改船体旋转改为相机环绕，画布任意位置均可拖动；保留键盘、惯性与俯仰限制。退出恢复进入前的相机位置和观察目标，小船不回退；清除原 OrbitControls 残余惯性，避免返回后漂移。
- 航行值继续遵循上一阶段规则，在个性化界面暂停累计。修改 `main.js`、`ship-showcase.mjs`、`template.html`，重建开发预览，历史 HTML 不变。
- 验证：36 项自动检查通过；新增实际 Three.js 相机回归验证跟随、环境视差、船体变换不被改写和退出还原。浏览器实测展示中航线时间从 34.0672 增至 44.8341，拖动改变场景视角，退出相机位置/目标与原值完全一致，控制台无警告或错误。
- 本次更新源码、开发预览和桌面打包前端输入，没有重新编译 EXE 或替换安装包。回退基点为 `b4237f9`。

## 2026-09-19 — 航行值第二阶段
- 正常航行每 60 秒积累 1 点，保留余数与历史总秒数；以单调时间差计时，5 秒卡顿一次结算，超过 30 秒或墙钟异常的间隔丢弃，不发离线收益。
- `main.js` 统一 `isSailingActive()`，展示进出、暂停、隐藏、最小化和上下文丢失停止计时；天气与帧率不影响兑换。航行值只在「我的船」面板上方显示小海浪和数字，无桌面资源栏或奖励提示。
- 新增 `sailing.mjs` 的加点、消费和余额校验接口，拒绝非法金额与透支；消费不减少历史时间。当前没有接入购买或奖励玩法。
- `game-save.mjs` 将外观与航行数据统一为版本 1 存档，迁移旧外观并保留旧键；周期 25 秒、整数变化、停航、外观修改、退出展示、关闭页面时保存。托盘退出加入先保存再退出及 2 秒兜底。
- 开发加速参数 `?sailingDebug=fast` 使用 5 秒兑换和独立测试存档；桌面构建强制 60 秒并关闭开发日志。前端实现仍由根目录源码生成 `preview/index.html`，历史 HTML 未改。
- 验证：35 项 JS 自动检查通过；浏览器观察正常航行获得 1 点、展示 102 秒不增长、暂停与刷新恢复、560×540 小资源标记布局；控制台无警告或错误。原生 release 构建通过，托盘退出持久化与桌面重启仍待实机交互验收，详见 `TEST_RESULTS.md`。
- 回退：上一阶段提交 `d9146bc`；旧外观键只保留迁移时数据，回退前如需保留新进度请备份 `tiny-tides-game-v1`。本次不替换 outputs 中的交付安装包，不更新线上站点。

## 2026-09-19 — 点击小船与「我的船」第一阶段
- 复用原场景和真实航行船，新增约 0.85 秒进退镜头；展示中冻结航线、停止新尾流，海水和环境继续；退出从原位置继续航行。
- 新增预设配色、3 个可用测试徽章与 3 个锁定占位、船名宽度限制；同一份外观数据实时影响航行船并写入 localStorage。船名/徽章在双侧铭牌上显示，复用一张纹理。
- 展示拖动只旋转船体，禁用原 OrbitControls；提供关闭、Escape、键盘旋转、页签键盘切换和减少动态效果支持。沿用轻量透明面板；没有加入后续经济、日志或解锁系统。
- 原生端增加光标位置与船体命中桥接；默认穿透状态下仅船体接收点击，展示结束恢复原编辑/锁定状态。悬停不主动抢焦点。
- 主要文件：`ship-customization.mjs`、`ship-showcase.mjs`、`main.js`、`template.html`、`wake-pool.mjs`、`desktop-bridge.mjs`、`src-tauri/src/main.rs`；生成预览为 `preview/index.html`。
- 验证：24 项 JS 检查通过；浏览器实测船体点击、独立旋转、配色/徽章/船名、刷新恢复、关闭/Escape 与手动暂停状态；560×540、390×700 面板无船体遮挡，控制台无警告/错误。桌面类型检查通过，原生点击/多屏仍待实机验收。
- 保存与回退：外观仅使用新增键 `tiny-tides-ship-v1`，不覆盖原视角/窗口设置；可回看之前的 `41c960d`。本次更新开发源码与预览，不替换 outputs 中已交付的 EXE/安装包。
- 构建：`npx tauri build --no-bundle` 成功生成当前源码的 release EXE，未重新制作安装包。

## 2026-09-19 — 分离历史、开发预览与桌面打包输入
- 目的：让旧 HTML、当前未打包预览和最终桌面打包输入在 Git 中有清晰边界，避免误改旧版或生成文件。
- 变化：历史独立网页保存至 `history/html/`；当前源码的浏览器预览改为 `preview/index.html`；Tauri 单独从忽略提交的 `desktop-dist/index.html` 打包。
- 命令：`npm run preview` 自动重建并启动未打包预览；`npm run desktop:dev` 预览桌面窗口；`npm run desktop:build` 生成最终 EXE。
- 修改入口：游戏功能只改项目根目录的 `main.js`、`template.html` 和相关 `.mjs`；不直接编辑 `preview/`、`desktop-dist/` 或 EXE。
- 文件：`build.mjs`、`scripts/preview.mjs`、`package.json`、`src-tauri/tauri.conf.json`、`.gitignore`、`history/html/`、`preview/index.html`、`README.md`。
- 回退：此前桌面打包输入为 `dist/index.html`，对应提交 `2c2e557`；历史 HTML 本身未改内容。

## 2026-09-17 — Windows EXE 实际交付
- 用户安装 Visual Studio C++ 工作负载后，解除此前 link.exe 缺失阻塞；成功生成 Windows EXE 和包含离线 WebView2 的 NSIS 安装包。
- 增加每次原生启动首个采样的自动诊断保存，用于确认场景已完成加载和绘制；不改变现有模型与交互。
- 实际启动 outputs/Tiny-Tides.exe：首个 5 秒采样完成 135 帧，窗口可见、锁定、未暂停，进程持续响应。启动采样 26.91 FPS，不能视作稳态达标。
- 22 项前端测试、3 项独立 Rust 位置恢复测试、Rust 格式检查和正式构建通过。更新 README.md、TEST_RESULTS.md 与 qa/ 真实记录；忽略生成的原生测试程序和 Tauri schema。
- 尚未验收：真实壁纸透明合成、托盘/穿透交互、多显示器/DPI、长时间原生资源增长及安装/卸载。file:// 预览加载问题未定位；EXE 已独立验证启动成功。
- 回退可检出 700f6f2；本次未更新线上站点。

## 2026-09-17 — Tauri 2 桌面景观工程
- 保留现有 Three.js 模型和美术，增量加入透明无边框窗口、托盘、默认鼠标穿透、编辑/锁定、专用移动手柄、设置保存和多屏位置恢复；保留独立离线网页。
- 移除常驻天气按钮、页面背景和无限地面；加入独立昼夜与天气状态机、相邻加权天气、暴雨冷却/海鸥预警离场与夜间随机极光。
- 水面和极光形变移至顶点着色器，尾流改固定 200 实例，雨滴复用 1100 线段；加入 30/20 FPS 与像素预算、隐藏停止绘制、按实际时间推进及性能诊断。
- 修复录音加载时静音被旧请求覆盖、连续微小天气变化未更新混音、暴雨预警海鸥未完全淡出等问题。
- 关键文件：src-tauri/、desktop-bridge.mjs、environment*.mjs、render-budget.mjs、water-shader.mjs、wake-pool.mjs、main.js、template.html；说明和实测记录见 README.md、TEST_RESULTS.md、qa/。
- 验证：22 项自动检查、网页正式/QA 构建和 Rust 格式检查通过；浏览器观察晴天/暴雨/极光约 30 FPS、节能档约 20 FPS，也记录了宿主约 1 FPS 降频。6 分钟极光观察中渲染资源计数稳定；详见实测限制。
- 未完成：原生构建因缺少 link.exe 失败，C++ 工具自动安装被审批策略阻止。Windows 透明合成、托盘/穿透、真实多屏/DPI、长期内存与 NSIS 安装尚未验收，不交付或声称已生成 exe。
- 回退：原网页基线为 ddede61，可在独立目录检出；设置文件位于用户应用配置目录，需保留时先备份。未更新线上站点。

## 2026-09-17 — 回退开始界面
- 按要求恢复已删除云朵的第7版：移除开始界面和出船演出，打开即航行；环境音恢复手动点击听海开启。
- 保留海浪滑杆、加速海豚、多层极光与五段天气。运行文件及独立HTML与ed27824版本一致，保留完整历史记录。
- 验证：目标版本文件差异为空；复用第7版已通过的构建和16项回归检查，不重新构建。

## 2026-09-17 — Q弹开始航行界面
- 首屏仅显示圆润四字按钮，柔和模糊海景衬底；加入进场弹性、轻浮动、悬停与按压回弹，随后渐隐转入场景。
- 用户点击才解锁并开启低音量海浪录音，音频准备后小船延迟0.45秒弹性出现，尾流随后生成；开场前船与海鸥隐藏、天气计时冻结、其他控件不可聚焦。入场完成后恢复交互。
- 支持键盘与减少动态效果偏好；重复点击只启动一次；音频失败仍可进入，通过听海按钮重试。
- 关键文件：departure.mjs、main.js、template.html、scene.test.mjs、README.md、dist/index.html。
- 验证：18项测试通过，包含静音等待、重复启动、音频失败降级与入场曲线；浏览器验证点击前后音频/船体/天气状态、桌面和390px开场、键盘启航，无运行错误。独立只读审查未发现重要问题。实体手机及硬件扬声器听感未实测。

## 2026-09-16 — 移除云朵
- 按要求移除所有天气中的云朵几何、漂移动画与天气调色，清理对应无用参数。
- 关键文件：main.js、README.md、dist/index.html。
- 验证：构建、语法检查及16项既有回归测试通过；未新增测试。

## 2026-09-16 — 可调海浪与自然海豚速度
- 目的：增强海浪可见度，并让用户实时调整起伏；改善海豚游动过慢的感觉。
- 新增0%–200%的「海浪大小」滑杆，默认波幅为上一版2.2倍，几何与波纹明暗同步平滑变化；所有天气共用设置，边缘维持封闭。船只、尾流、涟漪和海豚使用相同波高。
- 海豚横穿时间从29秒缩短至10秒，错开入场间隔从3秒缩短至1.1秒，摆尾适当加快。
- 关键文件：main.js、motion.mjs、scene-effects.mjs、template.html、scene.test.mjs、README.md、dist/index.html。
- 验证：16项测试通过，含零波幅、最大波幅边界与海豚整群退出时间；浏览器验证滑杆极值、静水过渡、海豚及390px窄屏布局，无运行错误。

## 2026-09-16 — 微缩世界的四层极光演出
- 用四层不同高度、宽度、深度和速度的立体光幕替换单条极光；柔和褶皱、渐变色与缓慢移动的亮芯形成半透明丝绸感，边缘淡出，范围始终限制在海洋模型上空。
- 首次进入夜景必现：夜色与星点先出现，3.5秒后光幕逐层舒展，海面染色稍后跟随，约18秒完成；后续保留45%的随机极光。重复点击当前夜景保持首次必现标记。
- 共享慢速运动与调色使海面泛起青绿／紫蓝模糊流光；三个局部弱光源轻染船体，切面增加微弱色光，离开夜景同步淡出。
- 星点减为38颗并降低亮度，缩小月亮，压低夜间界面对比，保留暖色船灯与原有天气、声音、海鸥和海豚规则。
- 关键文件：aurora.mjs、main.js、motion.mjs、scene-effects.mjs、template.html、scene.test.mjs、dist/index.html。
- 验证：14项行为测试覆盖首次演出、四层几何边界、光照出现／消退及原有天气与音频回归；浏览器查看完整极光、海面流光、视角旋转及重选夜景。修正浏览器发现的着色器拼接换行问题与独立审查发现的负数高斯计算兼容性、重选夜景取消首次保证问题。

## 2026-09-16 — 丝滑柔波与模型内部生态
- 目的：保留现有构图、微缩边界、天气循环和交互，局部提升水面与角色表现。
- 水面长波减幅并减速，边缘衰减改为平滑曲线；移除细颗粒和高频交叉纹理，保留连续柔和明暗。暴雨仅轻微增加波幅，各天气均保持柔波。
- 星空缩小为模型正上方的84颗微光，极光收缩为模型内的小型弯曲光幕；移除随镜头旋转的天空组逻辑。
- 晴天四只海鸥，小雨／黄昏两只；暴雨与夜景全部隐藏，返回晴天淡入。材质独立，避免透明度影响船只或其他海鸥。
- 海豚采用弧形背部轮廓、明确长吻与眼睛、浅色腹部、弯曲背鳍和水平双叶尾；增强体量与错落起伏，切换天气时下潜退出。
- 关键文件：motion.mjs、main.js、scene-effects.mjs、scene.test.mjs、README.md、dist/index.html。
- 验证：11项行为测试通过，涵盖低幅波面连续性、天空边界／固定坐标、海鸥天气生命周期及原天气／音频回归；构建和语法检查通过。浏览器检查晴天海豚、暴雨无海鸥、夜景局部星空及视角旋转，无新增渲染错误；独立审查未发现重要问题。极光随机出现的分支与范围通过逻辑／几何验证，本轮未单独截取其出现瞬间。
## 2026-09-16 — 五段天气、自然录音与海上偶遇
- 海面改为无镜面高光的漫反射材质，移除亮白浪纹，用柔和波面层次和细粒度纹理表现海水；暴雨浪高约为晴天2.4倍。
- 海鸥改为多边形身体和翼面；云朵改为平滑球面组合。恢复晴天浅色、夜间近黑背景，并加入金色黄昏。
- 添加本地时间、天气倒计时、五段约60秒自动流转及手选重置计时。船只、尾流与镜头保持连续。
- 添加错落经过的海豚群、边缘潜水；小雨／暴雨与随机闪电；繁星和夜间45%概率延迟出现的轻薄极光。
- 用署名许可的海浪、雨、海鸥、雷声实地录音替换合成声音；低音量、频段柔化、交叠循环、夜间降响、延迟远雷与音量滑杆。录音内嵌HTML，新增来源说明与页面署名。
- 独立审查后修正逐帧浮点计时导致天气跳段、静音／隐藏后遗留排程雷声的问题；海豚间距、星空深度和黄昏颜色经视觉调整。
- 验证：9项行为测试通过，包括30/60/120/144fps五分钟完整循环与录音生命周期；构建通过；四段音频在浏览器成功解码，峰值测量均低于-8dBFS；桌面与窄屏界面检查通过。硬件听感与实体手机操作未实测。
## 2026-09-16 — 海浪、胶片海蓝与环境音
- 用连续平滑网格和多方向长波替代随机三角色块，加入细涟漪、弯曲浪尖泡沫；船体、尾流和雨滴涟漪使用同一海面高度。
- 调整为较柔和的青蓝海色；晴天使用浅绿色背景，雨天为灰绿，夜景为深绿。
- 海鸥简化为两笔白色弧翼，去掉身体、嘴和羽翼细节。
- 新增 ambient-audio.mjs，本地合成起伏海浪、间歇海鸥叫声、雨声；天气混音平滑过渡，夜晚更轻。新增「听海」按钮，默认静音。
- 后台音频淡出并暂停；返回后尝试恢复，浏览器音频中断会同步按钮状态。
- 验证：构建及语法检查通过；5 项测试通过，覆盖航线、天气连续性、波浪边界、天气混音和鸣叫采样；浏览器检查三种天气与声音开关，390×844 手机布局正常；修复海面着色器关键字冲突后未发现新增运行错误。
- 独立审查发现的后台音频恢复问题已修正。实体手机音频中断与听感未实测。

## 2026-09-16 — 微缩海洋场景
- 实现独立 Three.js 海洋模型、四层切面、白色轮船、连续航线、动态白色尾流和四只海鸥。
- 实现晴天、局部雨天、月夜平滑切换，保留航行进度及镜头。
- 提供 OrbitControls、恢复视角、响应式中文 UI、可选 WebMCP 天气控制和 WebGL 错误提示。
- 修正竖屏裁切、海面偏白、夜景边界过暗及分层接缝；补齐底座上沿。
- 关键文件：main.js、motion.mjs、template.html、build.mjs、scene.test.mjs、dist/index.html。
- 验证：构建成功；2 项行为测试通过；JavaScript 语法检查通过；浏览器验证三种天气、旋转、缩放、重置及桌面／手机布局，无渲染错误；WebMCP 正常输入生效，非法天气输入被拒绝且状态不变。
- 已进行独立代码审查并修正发现的几何接缝问题。实体手机触摸操作未实测。
