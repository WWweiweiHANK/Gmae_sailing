{
  "name": "tiny-tides",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:gm": "node build.mjs --gm",
    "test": "node --test scene.test.mjs desktop.test.mjs sailing.test.mjs colors.test.mjs badges.test.mjs encounters.test.mjs",
    "preview": "npm run build && node scripts/preview.mjs",
    "preview:gm": "npm run build:gm && node scripts/preview.mjs",
    "desktop:frontend": "node build.mjs --desktop",
    "desktop:frontend:gm": "node build.mjs --desktop --gm",
    "qa": "node build.mjs --qa",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:check": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "three": "0.180.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "esbuild": "0.25.10"
  }
}
